# Required / recommended:
# Protejează toate endpoint-urile și previne abuz de trafic
API_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# Optional:
# Forward requests through another proxy (HTTP/HTTPS/SOCKS5)
# PROXY_URL=socks5://user:pass@host:1080

# Logging / UI
ENABLE_STREAMING_PROGRESS=false
DISABLE_HOME_PAGE=false

# Optional: dacă platforma setează PORT automat (unele PaaS)
# În Dockerfile proiectul folosește PORT=8888 by default
# PORT=8888
